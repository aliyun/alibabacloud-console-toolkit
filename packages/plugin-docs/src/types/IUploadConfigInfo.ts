export default interface IUploadConfig {
  ossAccessKeyId: string;
  ossAccessKeySecret: string;
  ossBucket: string;
  ossRegion: string;
  ossDir: string;
  ossName: string;
  ossTag: string;
  uploadDir: string;
  consoleOSId: string;
}